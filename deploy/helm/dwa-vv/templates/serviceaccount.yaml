apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "dwa-vv.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "dwa-vv.labels" . | nindent 4 }}
  annotations:
    # Azure Workload Identity binding — ties this SA to a managed identity
    # that holds "Key Vault Secrets User" on the Key Vault.
    azure.workload.identity/client-id: {{ .Values.workloadIdentity.clientId | quote }}
    azure.workload.identity/tenant-id: {{ .Values.workloadIdentity.tenantId | quote }}
