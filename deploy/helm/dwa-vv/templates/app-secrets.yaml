# App secrets are created/updated by kubectl in the CI pipeline (not Helm)
# to avoid Helm --set comma-splitting mangling connection strings.
# See the "Create / refresh app secrets" step in deploy-azure.yml.
